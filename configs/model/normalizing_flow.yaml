defaults:
  - default
  - net: tarflow_large

_target_: src.models.normalizing_flow_module.NormalizingFlowLitModule
force_gaussian_loss: True

optimizer:
  lr: 1e-4
  betas:
    - 0.9
    - 0.95
  weight_decay: 4e-4

sampling_config:
  use_com_adjustment: True