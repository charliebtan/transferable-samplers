# @package _global_

defaults:
  - training/tarflow_adaln_2aa
  - override /trainer: ddp

data:
  batch_size: 512

trainer:
  accumulate_grad_batches: 4
  strategy: ddp_find_unused_parameters_true

logger:
  wandb:
    offline: true