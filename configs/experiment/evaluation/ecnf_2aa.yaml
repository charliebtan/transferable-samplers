# @package _global_

defaults:
  - override /model: flow_matching
  - override /data: 2aa
  - override /trainer: gpu

ckpt_path: "/network/scratch/t/tanc/self-consume-bg/logs/train/runs/ecnf_2aa_v1/checkpoints/last.ckpt"
train: False
task_name: eval

model:
  eval_seq_name: null
  sampling_config:
    use_com_adjustment: False
    batch_size: 8
    num_test_proposal_samples: 10_000
    clip_reweighting_logits: 0.002
    subset_idx: ${seed}
  net:
    channels: 128
    num_layers: 9
    encoding_dim: 4

data:
  batch_size: 128
  num_eval_samples: 10_000

trainer:
  limit_test_batches: 1
  limit_val_batches: 1
