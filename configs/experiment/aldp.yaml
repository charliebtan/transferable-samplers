# @package _global_

defaults:
  - override /model: flow_matching
  - override /model/net: egnn_dynamics_ad2_cat
  - override /data: aldp

model:
  sampling_config:
    num_proposal_samples: 1000
    num_test_proposal_samples: 10000

trainer:
  check_val_every_n_epoch: 50
  max_epochs: 1000

data:
  batch_size: 512
