_target_: src.data.transferable_peptide_datamodule.TransferablePeptideDataModule
data_dir: ${paths.data_dir}new
train_lmdb_prefix: train_medium
val_lmdb_prefix: new_val
test_lmdb_prefix: new_test
batch_size: 512 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 1
pin_memory: False
num_aa_max: 4
num_dimensions: 3
num_particles: 97        # Max number of particles
dim: 291                 # num_dimensions * num_particles  # TODO remove?
