_target_: src.data.dw4_datamodule.DW4DataModule
data_dir: ${paths.data_dir}
batch_size: 256 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [100_000, 400_000, 500_000]
num_workers: 2
pin_memory: True