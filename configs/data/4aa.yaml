_target_: src.data.transferable_peptide_datamodule.TransferablePeptideDataModule
data_dir: ${paths.data_dir}new
train_lmdb_prefix: train_medium_v2
val_lmdb_prefix: val
test_lmdb_prefix: test
batch_size: 512 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 1
pin_memory: False
num_aa_max: 4
num_aa_min: 4
num_dimensions: 3
num_particles: 93       # Max number of particles + at least a single padding token
dim: 279                # num_dimensions * num_particles 
