# @package _global_
#
defaults:
  - override /hydra/launcher: submitit_slurm

hydra:
  launcher:
    partition: h100
    cpus_per_task: 16
    qos: medium
    mem_gb: 24
    gres: gpu:1
    timeout_min: 1300
    array_parallelism: 2 # max num of tasks to run in parallel (via job array)
    setup:
      - "conda activate fast-tgb"
